# Runtime + dev dependencies. This mirrors pyproject.toml (the source of truth);
# `pip install -e ".[dev]"` installs the same set. Kept for convenience:
#   pip install -r requirements.txt

fastapi>=0.110,<1
uvicorn[standard]>=0.27
pydantic>=2.5,<3
pydantic-settings>=2.1
python-dotenv>=1.0
anthropic>=0.40
langgraph>=0.2
httpx>=0.27
numpy>=1.26

# Dev / test tooling.
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.5

# Optional embedding backends for semantic runbook retrieval. Without either,
# the retriever falls back to lexical (hashing) matching.
#   voyageai>=0.2               # used when VOYAGE_API_KEY is set (pip install ".[voyage]")
#   sentence-transformers>=2.2  # local model (all-MiniLM-L6-v2), no key (".[local-embeddings]")
